import React from "react";

// Placeholder until the parallax artwork is implemented.
// The previous version used useScroll with a ref that was never attached,
// which threw "Target ref is defined but not hydrated".
export function ParallaxCurves() {
  return null;
}
